<div align="center">

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="client/images/AbstractumVPN_Full_logo.svg">
  <img src="client/images/AbstractumVPN_Full_logo_light.svg" alt="AbstractumVPN" width="320">
</picture>

<br><br>

**VPN-клиент, который сам поднимает сервер.**

Даёте ему машину, которой владеете. Он ставит туда VPN по SSH и подключается.
Без аккаунтов, без подписок, без посредника.

[![Сборка](https://github.com/FFriends/AbstractumVPN/actions/workflows/abstractum-ci.yml/badge.svg)](https://github.com/FFriends/AbstractumVPN/actions/workflows/abstractum-ci.yml)
[![Релиз](https://img.shields.io/github/v/release/FFriends/AbstractumVPN?include_prereleases&label=%D1%80%D0%B5%D0%BB%D0%B8%D0%B7)](https://github.com/FFriends/AbstractumVPN/releases)
[![Лицензия](https://img.shields.io/badge/%D0%BB%D0%B8%D1%86%D0%B5%D0%BD%D0%B7%D0%B8%D1%8F-GPL--3.0-blue)](LICENSE)

[Скачать](https://github.com/FFriends/AbstractumVPN/releases) ·
[Как это работает](#как-это-работает) ·
[По шагам](#по-шагам) ·
[Сборка](#сборка)

[English](README.md)

</div>

---

## Зачем он нужен

Коммерческий VPN предлагает доверить компании ровно тот трафик, который вы пытаетесь укрыть.
Свой сервер эту компанию убирает, но обычно взамен требует вечера с SSH, Docker и конфигами.

AbstractumVPN проводит этот вечер за вас и после этого не мешается. Сервер ваш. Ключи ваши.
Наружу ничего не уходит.

## Как это работает

То, чем он отличается от прочих клиентов: **он разворачивает сервер сам.**

```
   вы                      ваш сервер                     интернет
   │                          │                              │
   │  1. вход по SSH,         │                              │
   ├─────────────────────────►│                              │
   │     установка Docker     │                              │
   │                          │                              │
   │  2. подключение          │                              │
   ├═════════════════════════►│─────────────────────────────►│
   │     шифрованный туннель  │                              │
```

Вводите адрес, логин и пароль или ключ. Клиент заходит по SSH, ставит Docker, если его нет,
собирает контейнер под выбранный протокол и подключается. Убрать всё обратно — одна кнопка.

## По шагам

### Сначала — главное: на сервер вы не ставите ничего

Это первое, обо что все спотыкаются, поэтому скажем прямо.

**На сервер вы ничего не скачиваете, ничего не запускаете и вообще на него не заходите.** Там
нечего устанавливать руками. Файлы со страницы загрузки — это программа для **вашего
компьютера**, и на сервер из них не попадает ни один байт.

Всё, что нужно серверу, туда положит сама программа: она подключится к нему по сети, выполнит
там нужные команды за вас и отключится. Ваше участие — ввести адрес сервера и пароль от него
в четыре поля.

Машин в этой истории две, и путать их не надо:

| | Что это | Что вы с ней делаете |
|---|---|---|
| **Ваш компьютер** | Windows или Linux, тот, за которым вы сидите | Скачиваете и ставите AbstractumVPN отсюда, со страницы релизов |
| **Сервер** | Чужой компьютер в дата-центре, который вы берёте в аренду | **Ничего.** Только узнаёте его адрес и пароль и вводите их в программу |

### Шаг 1. Взять сервер в аренду

Нужна услуга, которая называется **VPS** (виртуальный сервер) — это когда вам за деньги дают
кусочек чужого компьютера в аренду и полный доступ к нему. Стоит обычно от 3–5 долларов в
месяц. Провайдеров много, конкретных не советуем — мы ни с кем не сотрудничаем.

Что выбрать при заказе:

- **Операционная система — Linux.** Подойдёт Ubuntu, Debian, Fedora, CentOS, openSUSE или Arch;
  если не знаете, что брать, берите **Ubuntu** последней версии — это самый обычный выбор.
- **Расположение — страна, откуда вы хотите выглядеть в интернете.** Ближе к вам — быстрее.
- **Мощность — самая маленькая.** VPN почти не нагружает сервер.
- **Сервер должен быть пустой.** Не тот, где у вас уже что-то работает.

После оплаты провайдер пришлёт три вещи. Они и есть всё, что вам нужно:

| Что пришлют | Как выглядит | Куда это потом вводить |
|---|---|---|
| IP-адрес сервера | `203.0.113.17` | поле «Server IP address» |
| Имя пользователя | обычно `root` | поле с логином |
| Пароль | набор символов | поле с паролем |

Иногда вместо пароля дают файл-ключ (SSH-ключ) — программа принимает и его.

### Шаг 2. Поставить программу на свой компьютер

Скачайте файл со [страницы релизов](https://github.com/FFriends/AbstractumVPN/releases) и
запустите его — как любую другую программу.

| Ваш компьютер | Какой файл брать |
|---|---|
| Windows | `AbstractumVPN_*_windows_x64.exe` |
| Linux | `AbstractumVPN_*_linux_x64.run` |

Windows покажет предупреждение «неизвестный издатель» — так и будет, пока у проекта нет
платного сертификата подписи.

### Шаг 3. Ввести данные сервера

Откройте программу, нажмите **«Приступим»**. Программа спросит, откуда брать подключение —
выбирайте вариант про **свой сервер**, а не про готовую конфигурацию.

Дальше — четыре поля, и всё из письма провайдера:

1. **Server IP address** — адрес из письма, например `203.0.113.17`. Порт указывать не нужно:
   если провайдер не сказал иначе, программа сама возьмёт стандартный. Если сказал — допишите
   через двоеточие: `203.0.113.17:2222`.
2. **Логин** — обычно `root`.
3. **Пароль** — из письма. Или вставьте ключ целиком, вместе со строками `BEGIN` и `END`.
4. Нажмите **«Продолжить»**.

Программа подключится к серверу и проверит, что доступ есть и что сервер не занят другой
установкой. Если что-то не сходится — скажет что именно.

### Шаг 4. Выбрать протокол

Появится экран **«VPN-протокол»**. Если не знаете, что выбрать, берите **AmneziaWG** — он стоит
первым и подходит большинству. Позже можно добавить другие, сервер это выдержит.

### Шаг 5. Подождать

Дальше программа делает всё сама: заходит на сервер, ставит там Docker, собирает VPN, открывает
нужный порт и подключается. Обычно **от трёх до десяти минут**, дольше всего на медленном
сервере. Прогресс виден на экране, рядом есть кнопка отмены.

Когда полоса дойдёт до конца — VPN работает. Дальше можно не читать.

### Шаг 6. Потом: добавить сервис, например AmneziaDNS

Это уже не обязательно. Если захочется, чтобы запросы к сайтам тоже шли мимо провайдера:

**Настройки** → ваш сервер → вкладка **«Сервисы»** → **AmneziaDNS** → установить.

Снова несколько минут ожидания, снова ничего не надо настраивать руками. Так же ставится всё
остальное из таблицы ниже, по одному.

Соседняя вкладка **«Протоколы»** добавляет второй протокол, а **«Управление»** удаляет — один
контейнер или всё, что программа когда-либо на сервер положила.

### Если что-то пошло не так

Спросить можно в чате при канале [@AbstractumMind](https://t.me/AbstractumMind) — по-русски или
по-английски. Ссылка есть и в самой программе, в разделе «О программе».

## Что нужно от сервера

Точная версия [шага 1](#шаг-1-взять-сервер-в-аренду) — для тех, кому нужны требования, а не
советы по заказу. Любая Linux-машина, которой вы владеете, доступная по SSH. Руками настраивать
нечего.

| | |
|---|---|
| **Дистрибутив** | Любой с `apt-get`, `dnf`, `yum`, `zypper` или `pacman` и systemd. Debian, Ubuntu, Fedora, CentOS, openSUSE, Arch подходят |
| **Доступ** | SSH под `root` или под пользователем в группе `sudo` / `wheel` |
| **Docker** | Ставится сам, если его нет. Готовить ничего не надо |
| **Порты** | Открываются сами в брандмауэре хоста. По умолчанию: OpenVPN `1194`, WireGuard `51820`, AmneziaWG `55424`, XRay и Cloak `443`, IKEv2 `500` и `4500` UDP |

Перед установкой клиент смотрит на файл блокировки пакетного менеджера. Если на сервере уже
что-то ставится, он дожидается конца той установки, а не лезет вторым процессом. Пока ждёт —
пишет, чего именно, и даёт кнопку отмены; если блокировка держится дольше пяти минут,
останавливается с ошибкой, а не ждёт бесконечно.

## Протоколы

| | Когда брать |
|---|---|
| **AmneziaWG** | По умолчанию. Скорость WireGuard плюс обфускация, чтобы трафик не выглядел как WireGuard |
| **WireGuard** | Быстро и просто, если трафик никто не разглядывает |
| **OpenVPN** | Зрелый и всем понятный. Медленнее |
| **XRay** | Обход глубокого анализа пакетов, через REALITY |
| **IKEv2 / IPsec** | Встроен в Windows, драйверов не требует |

**Cloak и Shadowsocks больше нет.** Апстрим убрал оба ещё до нашей точки среза, развернуть их
нельзя. Сервер, где такой протокол ещё стоит, клиент распознаёт и показывает в списке, честно
сообщая, что протокол не поддерживается, — подключиться он не пытается.

`AmneziaWG` и `AmneziaDNS` намеренно сохранили свои имена: это компоненты, которые вы ставите
на свой сервер, а не наш бренд. Переименуй мы их — вы перестали бы узнавать в настройках то,
что реально развернули.

## Что ещё он умеет ставить

**По умолчанию не ставится ничего из этого.** Выбрали протокол — получили протокол, и только
его. Ниже то, что при желании можно добавить потом, по одной штуке; путь по интерфейсу — шаг 6
выше. Удаляется той же кнопкой.

| | |
|---|---|
| **AmneziaDNS** | Резолвер `unbound`, доступный только внутри туннеля, с пересылкой по DNS-over-TLS. Запросы перестают уходить провайдеру |
| **SFTP-хранилище** | Личное файловое хранилище на сервере |
| **SOCKS5-прокси** | Для приложений, которые умеют SOCKS, но не VPN |
| **MTProxy** | Прокси Telegram на вашем сервере — чтобы раздать доступ другим людям |
| **Telemt** | То же самое, но более новый прокси Telegram, написанный на Rust |
| **Сайт в Tor** | Поднимает на вашем сервере сайт на WordPress и публикует его как onion-адрес. Открыть такой адрес можно только Tor Browser — обычный браузер `.onion` не разрешает |

Инструкции, как поставить это руками, нет, потому что руками ставить нечего. Вы не заходите на
сервер сами и ничего туда не копируете: клиент открывает сессию SSH, собирает там контейнер и
закрывает её. Файлы со страницы загрузки — это десктопный клиент, на сервер из них не попадает
ничего.

## Установка

Какой файл брать — в [шаге 2](#шаг-2-поставить-программу-на-свой-компьютер) выше. Что стоит
знать сверх этого:

Пока проект молодой, каждый релиз выкладывается с пометкой pre-release. Ярлык GitHub «latest»
указывает только на стабильный релиз, поэтому здесь он не ведёт никуда — смотреть надо страницу
релизов целиком.

macOS, Android и iOS не собираются: код их поддерживает, а сертификатов подписи и аккаунтов в
магазинах нет.

Установленный клиент сам смотрит релизы этого репозитория и предлагает обновиться. Запрос
уходит без тела и не несёт ничего, по чему вашу установку можно было бы опознать.

## Сборка

**Локальной сборки нет.** Всё собирается в GitHub Actions, и это решение, а не упущение:
Qt 6.10 плюс девятнадцать зависимостей Conan из исходников — не тот тулчейн, который стоит
воспроизводить на каждой машине.

Цикл: правка, пуш, смотреть [Actions](https://github.com/FFriends/AbstractumVPN/actions).
Ежедневная задача в 03:00 UTC собирает всё новое; еженедельная, по воскресеньям в 09:00 UTC,
выкладывает релиз — и только если с прошлого раза появились коммиты.

Тулчейн можно поднять, если нужен отладчик: CMake 3.25+, Conan 2.x, Qt 6.10+ **с модулем Qt
Remote Objects** — в стандартную установку Qt он не входит, и без него привилегированная
служба не соберётся вовсе. Дальше `deploy/build.sh` или `deploy\build.bat`. Ничего в проекте
на этом не завязано.

## Безопасность

Об уязвимостях сообщайте через
[GitHub Security Advisories](https://github.com/FFriends/AbstractumVPN/security/advisories/new).
Обсуждение остаётся приватным, пока не выйдет исправление, и этот канал работает, даже когда
трекер задач закрыт. Что считаем своим, а что нет, и в какие сроки отвечаем —
[SECURITY.md](SECURITY.md).

Клиент состоит из двух частей: обычного приложения и службы с системными правами, которой
принадлежат маршруты, интерфейс туннеля и kill switch. Эта граница и есть то, что стоит
рассматривать пристально, — работа шла именно там:

- привилегированные каналы обслуживают клиент, установленный рядом со службой, и отказывают
  всем остальным; это проверяется на каждом соединении, а не предполагается;
- аргументы, уходящие в процессы с системными правами, проходят через белый список для
  каждого процесса, а незнакомая опция отменяет весь запуск, а не отбрасывается тихо.

Одно, что стоит знать заранее, потому что не исправлено:

- **На Linux список серверов не шифруется.** В апстриме это отключили, потому что keychain там
  работает ненадёжно, и мы это пока не заменили. Адреса серверов и ключи лежат в `QSettings`
  открытым текстом.

## Участие в разработке

См. [CONTRIBUTING.md](CONTRIBUTING.md). Коротко: коммиты в формате `тип: описание`, работа
идёт в `dev`, сборка только в CI.

Трекер задач пока закрыт. Об уязвимостях сообщайте по ссылке выше, правку присылайте
pull request'ом, а с вопросом приходите в чат Telegram — он ниже.

## Где про это пишут

[**@AbstractumMind**](https://t.me/AbstractumMind) в Telegram — что делается, что ломается по
дороге и всё остальное, что кажется стоящим записать. Новости на русском.

К каналу привязан чат, и написать туда можно по любому поводу, включая помощь с клиентом, —
на русском или английском. Другие языки тоже работают, но между сторонами будет переводчик, и
часть смысла по дороге теряется.

## Лицензия и происхождение

GPL-3.0, см. [LICENSE](LICENSE) и [NOTICE](NOTICE).

AbstractumVPN основан на
[amnezia-vpn/amnezia-client](https://github.com/amnezia-vpn/amnezia-client), влит до среза
`0d116bfc`. Большая часть кода написана ими, и лицензия унаследована, а не выбрана: GPLv3
этого требует.

Проект независимый, с Amnezia не связан и ими не поддерживается. **О проблемах с
AbstractumVPN сообщайте сюда, а не им.** Чем отличается: нет подписок, нет платного хостинга,
нет рекламы, нет аккаунта и нет обращений к чужим серверам — проверка обновлений идёт в этот
репозиторий, всё остальное на машину, которую вы подняли сами.

Сторонние компоненты и их лицензии перечислены в
[THIRD_PARTY_LICENSES.md](THIRD_PARTY_LICENSES.md).
